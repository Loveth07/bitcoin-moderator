[project]
name = "bitcoin-moderator"
authors = ["Stacks Community"]
description = "Bitcoin-based goal moderation and verification platform"
telemetry = false
requirements = []
[contracts.bitcoin-moderator]
path = "contracts/bitcoin-moderator.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
